package com.hugg.domain.model.enums

enum class TopBarLeftType {
    BACK, LOGO, CLOSE, NONE
}

enum class TopBarMiddleType {
    LOGO, TEXT, NONE
}

enum class TopBarRightType {
    SKIP, DAILY_RECORD, CREATE, CHECK, DELETE, DELETE_GS30, NOTIFICATION, POINT, NONE
}